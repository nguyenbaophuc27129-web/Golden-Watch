# PSCS Golden-Watch v8.0 — dependencies (Python 3.10+, Windows)
# Cài: pip install -r requirements.txt

# --- Vision / pose ---
mediapipe>=0.10.9
ultralytics>=8.0.0        # YOLOv8-pose (M3 arm, M4 gait)
opencv-python>=4.8.0
numpy>=1.24.0

# --- ML ---
torch>=2.0.0
scikit-learn>=1.3.0
joblib>=1.3.0

# --- Speech (M2) ---
sounddevice>=0.4.6        # thu âm (thay pyaudio)
soundfile>=0.12.1
librosa>=0.10.1           # trích 48 features
vosk>=0.3.45              # speech-to-text tiếng Việt (models/vosk-model-vn-0.4)

# --- Radar M5 ---
pyserial>=3.5             # LD2450 UART-USB 256000 baud

# --- App gia đình (Streamlit) ---
streamlit>=1.30.0

# --- Handoff: QR + PDF ---
qrcode[pil]>=7.4
reportlab>=4.0.0

# --- Science table Excel (SYS-06) ---
openpyxl>=3.1.0
